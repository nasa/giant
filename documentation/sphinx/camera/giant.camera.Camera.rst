Camera
======

.. currentmodule:: giant.camera

:mod:`giant.camera`\:

.. autoclass:: Camera
    :no-members:
    :members: model, images, image_mask, psf, attitude_function, start_date, end_date, name, spacecraft_name

   
.. rubric:: Summary of Methods

.. autosummary::
  :nosignatures:

  ~Camera.add_images
  ~Camera.short_on
  ~Camera.short_off
  ~Camera.only_short_on
  ~Camera.long_on
  ~Camera.long_off
  ~Camera.only_long_on
  ~Camera.all_on
  ~Camera.all_off
  ~Camera.sort_by_date
  ~Camera.apply_date_range
  ~Camera.image_check
  ~Camera.preprocessor
  ~Camera.remove_images
  ~Camera.update_attitude_from_function
  ~Camera.update_short_attitude


|
